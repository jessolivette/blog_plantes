<!doctype html>
<html lang="fr">
    <head>
      <meta charset="utf-8">
      <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>My Blog</title>
        <link rel="stylesheet" type="text/css" href="{{ '/css/blogPlantes.css' | url }}">
        <link rel="stylesheet" type="text/css" href="{{ '/css/fichePlante.css' | url }}">
        <link href="https://fonts.googleapis.com/css?family=Berkshire+Swash" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css?family=Montez|Tangerine" rel="stylesheet">
        <style type="text/css">
        body {
          background-image: url("{{ background | url }}");
          background-size: cover;
        }
        </style>
    </head>
    <body>
      <header>
        <h1>{{ pageTitle }}</h1>
      </header>
      <div class="section">
        <h3>{{ subTitle }}</h3>
        {{ content }}
      </div>
      <button><a href="{{ '/' | url }}#{{ pageTitle }}" id="homeLink">Accueil</a></button>
    </body>
</html>
